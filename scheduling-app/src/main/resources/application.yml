server:
  # HTTP 与机器人 TCP 必须分端口；机器人 TCP 端口由 Action 模块常量定义为 8080。
  port: 8081

spring:
  application:
    name: kunling-scheduling
  datasource:
    url: jdbc:mysql://39.105.68.15:50013/kunling?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: sunder
    password: "123456qwertQ"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jackson:
    default-property-inclusion: non_null
    deserialization:
      fail-on-unknown-properties: true
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
  flyway:
    enabled: true
