# Action 数据库维护

- `create/kunling_action_schema.sql` 是不可变建库基线，禁止修改；任何表结构或种子数据变更都必须新增到 `alter/`。
- 全新数据库先执行不可变基线，再按时间顺序执行全部 `alter/` 脚本。
- 已有数据库只执行尚未应用的 `alter/` 脚本，执行前备份并记录环境、操作人和结果。
- 应用启动不建表、不修表、不自动补写初始数据；本项目不引入 Flyway。
- ALTER 脚本执行后不得修改，新变更必须追加新脚本。
